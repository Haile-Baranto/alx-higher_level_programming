#!/usr/bin/node
/**
 * Update this script to replace the value 12 with 89:
 * You are not allowed to use var
 * the following code is given
 */
const myObject = {
  type: 'object',
  value: 12
};
console.log(myObject);
myObject.value = 89; // line of code that changes value of value to 89
console.log(myObject);
