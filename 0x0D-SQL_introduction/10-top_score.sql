-- This script lists all records of the table second_table from the hbtn_0c_0 
-- database, ordered by score (top first).

-- List all records ordered by score
SELECT score, name FROM second_table ORDER BY score DESC;
